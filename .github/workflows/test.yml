name: Test

on:
  workflow_dispatch: {}

jobs:
  deploy:
    name: Deploy to Netlify
    uses: ./.github/workflows/upload-build-to-repo.yml
    secrets: inherit
    with:
      artifact_name: WebGL-Build-3e84b55
      repository_path: epen3420/epen_page
      run_id: 19258933860
