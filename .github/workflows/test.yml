name: Test

on:
  workflow_dispatch: {}

jobs:
  deploy:
    name: Deploy to Netlify
    uses: ./.github/workflows/upload-build-to-repo.yml
    secrets: inherit
    with:
      artifact_name: WebGL-Build-ea9bd5d
      repository_path: epen3420/epen_page
      run-id: 19257445305
